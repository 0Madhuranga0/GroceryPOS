<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Read the log directory from app.properties via system property set at startup.
         Falls back to "logs" if the property is not set. -->
    <property name="LOG_DIR" value="${log.dir:-logs}"/>
    <property name="APP_NAME" value="GroceryPOS"/>

    <!-- =====================================================================
         Console Appender
    ===================================================================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- =====================================================================
         Rolling File Appender — daily rotation, keep 30 days
    ===================================================================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${APP_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Roll over daily -->
            <fileNamePattern>${LOG_DIR}/${APP_NAME}.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- =====================================================================
         Suppress noisy library loggers
    ===================================================================== -->
    <logger name="net.sf.jasperreports" level="WARN"/>
    <logger name="com.mysql" level="WARN"/>
    <logger name="org.apache" level="WARN"/>

    <!-- =====================================================================
         Application loggers — DEBUG in development, change to INFO for prod
    ===================================================================== -->
    <logger name="com.gp.grocerypos" level="DEBUG"/>

    <!-- =====================================================================
         Root logger
    ===================================================================== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
